
let log = function(message){ //declare a function i javascript
    console.log(message);
}

let doLog = (message) => console.log(message); //declare it in a faster was in typescript, it is called an arrow function

let doSome = () => console.log(); //it also can be empty


